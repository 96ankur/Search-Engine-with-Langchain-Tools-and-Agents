# Search-Engine-with-Langchain-Tools-and-Agents
This is a search engine application, developed using Langchain Tools and Agents

**NOTE: This application uses UV Package manager**

## Steps to start the application
1) Clone the repository
2) Run `uv sync`
3) Run `streamlit run app.py`